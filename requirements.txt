# Flask and web dependencies
Flask>=2.3.0
Werkzeug>=2.3.0
python-dotenv>=1.0.0
humanize>=4.0.0

# Semantic search (optional - server still runs without these, search just disables)
# Embeddings: BAAI/bge-small-en-v1.5  |  Rerank: cross-encoder/ms-marco-MiniLM-L-6-v2
sentence-transformers>=2.2.0
torch>=2.0.0
numpy>=1.24.0

# PDF text extraction (optional)
pypdf>=3.0.0

# Analytics uses the stdlib sqlite3 module (no dependency).
# Keyword search (BM25) is implemented in-tree (no dependency).

# Development
# pytest>=8.0.0
